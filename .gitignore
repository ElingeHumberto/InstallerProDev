# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
pip-log.txt
pip-delete-this-directory.txt
.tox/
.coverage
.cache
*.egg-info/
.pytest_cache/
htmlcov/
.ipynb_checkpoints
.mypy_cache/
.dmypy.json
.ruff_cache/
celerybeat-schedule
.vscode/ # Ignorar configuraciones de VS Code específicas de tu máquina

# OS
.DS_Store
.Trashes
ehthumbs.db
Thumbs.db
*.suo
*.user
*.bak
*.obj
*.pdb
*.log
build/
dist/
installerpro/*.spec # Si usas PyInstaller, ignorar el archivo .spec

# Application specific data (user-specific, generated during runtime)
# These paths are generated by appdirs. You should NOT commit these to Git.
# They will be created automatically on each user's machine.
# Ensure these paths match what appdirs generates on your system.
# Adjust if your actual appdirs paths are different.
# Example for Windows:
C:/Users/*/AppData/Roaming/InstallerPro/
C:/Users/*/AppData/Local/InstallerPro/
# Example for Linux/macOS (if you use those):
# ~/.config/InstallerPro/
# ~/.local/share/InstallerPro/

# If you want to temporarily ignore an specific file that is already tracked (e.g., for local testing changes)
# you can use `git update-index --assume-unchanged <file>`
# To revert: `git update-index --no-assume-unchanged <file>`